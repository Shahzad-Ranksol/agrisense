FROM node:20-alpine AS builder
WORKDIR /app
COPY frontend/package*.json ./
RUN npm install --frozen-lockfile
COPY frontend/ ./
COPY .env .env
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx/agrisense.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
